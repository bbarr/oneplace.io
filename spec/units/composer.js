var composer = require('../../app/lib/composer');
var assert = require("assert")

describe('Composer', function() {

  var adapters, user;
  beforeEach(function() {
    adapters = { foo: {} };
    user = { keys: { foo: {} } };
  });

  describe('#compose', function() {
     
  });
})
